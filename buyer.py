def buy_token(token_address, wallet_key, amount):
    print(f"💸 Executing buy: {token_address} | Amount: {amount}")
    # Real buy logic would be here (e.g., transaction with Phantom wallet via RPC)
